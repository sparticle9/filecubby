BOT_TOKEN=
ADMIN_TOKEN=

# Optional Worker secret/env binding. Leave blank only for a private bot DM
# after sending one message, such as /start, to the bot. Set explicitly for
# groups, channels, or bots visible in multiple chats.
CHAT_ID=

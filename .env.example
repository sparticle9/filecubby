BOT_TOKEN= # Required. Telegram bot token from @BotFather.
ADMIN_TOKEN= # Required. Private admin bearer token, for example from `openssl rand -hex 32`.
CHAT_ID= # Optional. Leave blank only for a private bot DM after sending /start to the bot. Set explicitly for groups, channels, or bots visible in multiple chats.
